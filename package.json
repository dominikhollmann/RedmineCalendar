{
  "name": "redmine-calendar",
  "version": "1.0.0",
  "description": "Outlook-style weekly calendar for Redmine time tracking",
  "scripts": {
    "proxy": "lcp --proxyUrl https://demo.redminecloud.net --port 8010",
    "serve": "npx serve ."
  },
  "devDependencies": {
    "local-cors-proxy": "^1.1.0",
    "serve": "^14.0.0"
  }
}
