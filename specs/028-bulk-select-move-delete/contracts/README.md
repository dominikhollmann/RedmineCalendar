# Contracts: Bulk Multi-Select for Move and Delete

This feature exposes **no external interfaces**. It is an internal calendar interaction layered on top of existing Redmine API methods (`updateTimeEntry`, `deleteTimeEntry`).

The internal JS contracts (`selection.js`, `bulk-actions.js`) are documented in `data-model.md`.
