# Contracts: Dark Mode (Settings-Only Toggle)

This feature exposes **no external interfaces**. It is a purely visual feature with no API or data-format implications.

The internal JS module contract (`js/theme.js`) and the cross-feature inheritance contract for feature 031 are documented in `data-model.md`.
