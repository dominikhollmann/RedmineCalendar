# Contracts: Time Entry Anomaly Detection

This feature exposes **no external interfaces**. It is a pure-UI overlay on top of already-loaded calendar data. No new API calls, no new data formats.

The internal JS contract (`detectAnomalies`) is documented in `data-model.md`.
