installed: []
settings:
  auto_execute_hooks: true
hooks:
  before_constitution:
  - extension: git
    command: speckit.git.initialize
    enabled: true
    optional: false
    prompt: Execute speckit.git.initialize?
    description: Initialize Git repository before constitution setup
    condition: null
  before_specify:
  - extension: git
    command: speckit.git.feature
    enabled: false
    optional: false
    prompt: Execute speckit.git.feature?
    description: Create feature branch before specification (disabled - specs+plan
      live on main when feature.json pins the directory; setup-plan.sh enforces this
      via feature_json_matches_feature_dir).
    condition: null
  after_specify:
  - extension: github-issues
    command: speckit.github-issues.create
    enabled: true
    optional: false
    prompt: Create GitHub Issue for this feature?
    description: Auto-create the canonical feature Issue (replaces BACKLOG.md row append).
    condition: null
  after_clarify:
  - extension: github-issues
    command: speckit.github-issues.update-status
    enabled: true
    optional: false
    prompt: Update GitHub Issue status to clarify?
    description: Transition the feature Issue to status:clarify.
    args:
      status: clarify
    condition: null
  after_plan:
  - extension: github-issues
    command: speckit.github-issues.update-status
    enabled: true
    optional: false
    prompt: Update GitHub Issue status to plan?
    description: Transition the feature Issue to status:plan.
    args:
      status: plan
    condition: null
  after_tasks:
  - extension: github-issues
    command: speckit.github-issues.update-status
    enabled: true
    optional: false
    prompt: Update GitHub Issue status to tasks?
    description: Transition the feature Issue to status:tasks.
    args:
      status: tasks
    condition: null
  after_implement:
  - extension: github-issues
    command: speckit.github-issues.update-status
    enabled: true
    optional: false
    prompt: Update GitHub Issue status to implement?
    description: Transition the feature Issue to status:implement.
    args:
      status: implement
    condition: null
  - extension: git
    command: speckit.git.test
    enabled: true
    optional: false
    prompt: Run unit and UI tests to verify implementation?
    description: Run test suites as final implementation quality gate
    condition: null
